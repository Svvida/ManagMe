export enum Role {
  Admin = "admin",
  DevOps = "devops",
  Developer = "developer",
}

export enum Priority {
  Low = "low",
  Medium = "medium",
  High = "high",
}

export enum Status {
  Todo = "todo",
  Doing = "doing",
  Done = "done",
}
